```mermaid
sequenceDiagram
    actor UsuarioNaoLogado
    participant Usuario as Usuario
    participant handleValidacao as handleValidacao
    participant DB as DB

    activate Usuario
    UsuarioNaoLogado->>Usuario: login = +realizarLogin(email: string, senha: string)
    activate Usuario
    Usuario->>handleValidacao: +autenticar(email: string, senha: string)
    activate handleValidacao
    deactivate Usuario
    handleValidacao->>handleValidacao: +validarDados(email: string, senha: string)
    activate DB
    handleValidacao->>DB: validarDB = + validarDadosDB(email: string, senha: string)
    DB->>handleValidacao: validarDB
    deactivate DB
    handleValidacao->>Usuario: validarDB
    activate Usuario
    deactivate handleValidacao
    Usuario->>UsuarioNaoLogado: login
    deactivate Usuario
