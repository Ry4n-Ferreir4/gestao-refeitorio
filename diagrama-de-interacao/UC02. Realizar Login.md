```mermaid
sequenceDiagram
    actor UsuarioNaoLogado
    participant Usuario as Usuario
    participant handleValidacao as handleValidacao
    participant DB as DB

    activate Usuario
    UsuarioNaoLogado->>Usuario: +realizarLogin(email: string, senha: string)
    activate Usuario
    Usuario->>handleValidacao: +autenticar(email: string, senha: string)
    activate handleValidacao
    deactivate Usuario
    handleValidacao->>handleValidacao: +validarDados(email: string, senha: string)
    activate DB
    handleValidacao->>DB: + validarDadosDB(email: string, senha: string)
    DB->>handleValidacao: + validarDadosDB : Boolean
    deactivate DB
    handleValidacao->>Usuario: + validarDadosDB : Boolean
    activate Usuario
    deactivate handleValidacao
    Usuario->>UsuarioNaoLogado: +realizarLogin() : Boolean
    deactivate Usuario
