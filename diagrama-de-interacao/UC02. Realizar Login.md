```mermaid
sequenceDiagram
    participant Usuario as Usuário não Logado
    participant Sistema as Sistema
    participant EmailService as Serviço de E-mail

    Usuario->>Sistema: +realizarLogin(email: string, senha: string) : : Boolean
    Sistema->>Sistema: +validarDados(email: string, senha: string)
        Sistema->>EmailService: +enviarEmailConfirmacao(email: string)
        EmailService-->>Usuario: Recebe e-mail de confirmação
        Usuario->>Sistema: +confirmarLogin(email: string)
        Sistema-->>Usuario: +loginBemSucedido() : Boolean
   
