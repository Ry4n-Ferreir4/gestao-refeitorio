```mermaid
sequenceDiagram
    participant Usuario as Administrador/Gestor
    participant Sistema as Sistema
    participant BancoDeDados as Banco de Dados

    Usuario->>Sistema: editarCardapio(cardapio: Cardapio)
    Sistema->>Usuario: listarCardapios(cardapio: Cardapio)
    Usuario->>Sistema: selecionarCardapio(cardapioId: String)
    Sistema->>Usuario: exibirCamposEdicao(cardapio: Cardápio)
    Usuario->>Sistema: alterarDadosCardapio(novosDados: Dados)
    Usuario->>Sistema: salvar()
    Sistema->>Sistema: validarDados(novosDados: Dados)
    Sistema->>BancoDeDados: atualizarCardapio(cardapioId: Dtring, novosDados: Dados)
    BancoDeDados-->>Sistema: confirmarAtualizacao()
    Sistema-->>Usuario: mensagemSucesso()
